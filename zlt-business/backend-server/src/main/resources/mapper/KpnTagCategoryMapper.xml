<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.central.backend.mapper.KpnTagCategoryMapper">
    <select id="findList" resultType="com.central.backend.model.vo.KpnTagCategoryVO">
        select * from kpn_tag_category t
        LEFT JOIN
        <if test="headquarters == null || headquarters == '' || headquarters == '1'">
            (SELECT mt.tag_category_id,count(mt.movie_id) FROM kpn_movie_tag mt LEFT JOIN kpn_movie m ON mt.movie_id= m.id WHERE m.status='1' GROUP BY mt.tag_category_id) s
        </if>
        <if test="headquarters != null and  headquarters != '' and  headquarters == '0'">
            (SELECT mt.tag_category_id,count(mt.movie_id) FROM kpn_movie_tag mt LEFT JOIN  kpn_site_movie sm ON mt.movie_id= sm.movie_id LEFT JOIN kpn_movie m ON sm.movie_id= m.id WHERE sm.status='1' and sm.site_id = #{siteId}  GROUP BY mt.tag_category_id) s
        </if>
        ON a.id=s.tag_category_id
        <where>
            1=1
            <if test="id != null and  id != '' and  id != '0'">
                and t.id = #{id}
            </if>
            <if test="name != null and  name != ''">
                and t.name LIKE CONCAT('%',#{name},'%')
            </if>

        </where>
    </select>

    <select id="findListAll" resultType="com.central.common.model.KpnTagCategory">
        select * from kpn_tag_category t
    </select>
</mapper>